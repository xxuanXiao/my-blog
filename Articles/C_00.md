# C语言知识点

> 2026年04月16日 创建

🔥 核心基础（必须掌握）
static关键字
数据类型与内存大小char/short/int/long/long long、float/double、有无符号、溢出问题
指针（重中之重）指针含义、指针运算、野指针、空指针、多级指针、指针与数组
数组与字符串数组越界、字符数组与字符串区别、strlen/sizeof 区别
函数与参数传递值传递、地址传递、函数指针、回调函数
内存四区模型代码段、数据段、bss、栈、堆（理解 static 的基础）
🚀 进阶核心（面试高频）
动态内存管理 malloc/free/calloc/realloc内存泄漏、野指针、重复释放
结构体、共用体、枚举结构体内存对齐（超级重点）、位段、union 大小判断
typedef 与 #define区别、使用场景、坑点
extern 关键字多文件变量 / 函数声明、全局变量跨文件引用
const 关键字常量指针、指针常量、顶层 const / 底层 const
volatile 关键字防止编译器优化，嵌入式必考
预处理指令#include、#define、#ifdef、#ifndef、#pragma
⚙️ 高级 / 工程知识点
文件操作fopen/fread/fwrite/fclose、文本 / 二进制模式
大小端字节序判断方法、网络字节序
函数指针与回调常用于驱动、框架、状态机
柔性数组结构体最后一个成员不指定大小
goto 的合理使用统一资源释放场景
错误处理与 errno
🧪 嵌入式 / 底层必学
位运算& | ~ ^ <<>> 、置位、清位、取位
中断、寄存器操作